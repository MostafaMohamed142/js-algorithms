// arrays are collection of mixed value / could be (numbers,strings,object,booleans,etc)
// arrays are iterable (expected values to be returned), so we can use for of loop to access it values
// we can access arrays by its index starting from 0

const arr=['mostafa',1,10,{fname:'mohamed',lname:'ahmed',job:'frontend developers'},20];

// for(item of arr){
//     console.log(item)
// }
// output
// mostafa
// 1
// 10
// { fname: 'mohamed', lname: 'ahmed', job: 'frontend developers' }
// 20
// arr.push(new Object({
//     name:"John",
// })) // adding a value at the end of the array
// 

// let popped=arr.pop() // remove an item from the end of the array , return the popped element
// console.log(arr);
// console.log(popped)

// arr.unshift(5) // add item at the begining of the array
// arr.shift() // remove the item an the beginning of the array
//// there's alot of array methods [toStrin(),find(),findIndex(),flat(),concat(),slice(),splice(),join()]

